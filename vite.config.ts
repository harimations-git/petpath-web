import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// Vite configuration for the PetPath React web application.
// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
})
